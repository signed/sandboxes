apply plugin: 'war'

dependencies {
    compile project(':shared')
    providedCompile "javax.servlet:servlet-api:2.5"
}

war {
    manifest {
        attributes 'Dependencies': 'org.example.alpha'
    }
}
