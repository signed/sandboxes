{
  "name": "oauth-sandbox",
  "version": "1.0.0",
  "description": "just try to get an oauth flow going",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "express": "4.16.3",
    "request": "2.87.0"
  }
}
